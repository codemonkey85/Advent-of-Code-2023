using Advent2023.Library.Days;
await Solver.Solve<Day06>(options => options.ProblemAssemblies = [typeof(BaseLibraryDay).Assembly]); // .SolveAll(options => options.ProblemAssemblies = [typeof(BaseLibraryDay).Assembly]);
